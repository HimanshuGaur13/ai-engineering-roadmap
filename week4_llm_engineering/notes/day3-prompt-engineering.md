Zero-shot = Task without examples.
One-shot = One example before asking the model.

Few-shot = Multiple examples to teach the model.

System Prompt = System Prompt controls model behavior.

Prompt Template = Reusable prompt with variables.

Chain of Thought (CoT) = Step-by-step reasoning

Structured Prompt = Structured prompts produce structured outputs.

RAG Prompt = Answer only from context